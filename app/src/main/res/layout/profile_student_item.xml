<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Column for displaying the enrollment details (course name, time, date) -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingEnd="16dp">

        <TextView
            android:id="@+id/enrollment_course"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Course Name"
            android:textSize="18sp"
            android:textColor="#000000"/>

        <TextView
            android:id="@+id/enrollment_time_slot"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Time Slot"
            android:textSize="16sp"
            android:textColor="#555555"/>

        <TextView
            android:id="@+id/enrollment_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Date"
            android:textSize="14sp"
            android:textColor="#555555"/>
    </LinearLayout>

    <!-- Cancel button to remove the enrollment -->

    <Button
        android:id="@+id/btnViewTutorProfile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View Student"
        android:textColor="#007BFF"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="16dp"/>
</LinearLayout>
